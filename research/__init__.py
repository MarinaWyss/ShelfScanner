"""Research tooling for the spike: matrix drivers and the comparison reports.

Nothing in here is part of the scan pipeline; it reads the rows the pipeline
logs and turns them into comparisons. Run modules from the repo root with
`uv run python -m research.<module>`.
"""
