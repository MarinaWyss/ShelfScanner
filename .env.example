SUPABASE_URL=
SUPABASE_SECRET_KEY=
OPENROUTER_API_KEY=
# Direct provider adapters (change 002). Each is only needed when a model in
# config/models.toml names that adapter.
GEMINI_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
# Model-calling commands refuse to run once the runs tables sum past this
# amount since settings.spend_since in config (change 002, task 0).
SHELFSCANNER_SPEND_CAP_USD=
# Days a session photo is kept before the retention job deletes the object (008); default 30.
SHELFSCANNER_RETENTION_DAYS=
# Scan limits in the web app (008 D1). Scans a device may start per rolling hour; default 10.
SHELFSCANNER_SCANS_PER_HOUR=
# The app's spend across every session per UTC day, summed from the runs tables; default 5.
SHELFSCANNER_APP_DAILY_CAP_USD=
# 1 runs the web app on the fake pipeline and in-memory sessions (003): no project, no keys, fixed titles.
SHELFSCANNER_FAKE_PIPELINE=
# Shared secret for the /admin dashboard (009); unset means the page is always a 404.
SHELFSCANNER_ADMIN_SECRET=
