[project]
name = "shelfscanner"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "MarinaWyss", email = "marina.bennett.wyss@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "python-dotenv>=1.2.3",
    "supabase>=2.31.0",
]

[project.scripts]
shelfscanner = "shelfscanner:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
