# Lint config (change 002, task 0). Kept minimal on purpose: ruff's defaults plus
# import order, pyupgrade and bugbear. Line length is not enforced; the report
# and the HTML page are written wide deliberately.
target-version = "py312"
line-length = 130

[lint]
select = ["E4", "E7", "E9", "F", "W", "I", "UP", "B"]
