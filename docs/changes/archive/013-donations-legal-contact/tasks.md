# 013 — Tasks

- [x] 1. Routes and unsessioned paths for `/privacy-policy`, `/terms-conditions`, `/contact`.
- [x] 2. `privacy.html` and `terms.html` from D4 and D5; `.prose` styles.
- [x] 3. `contact.html` with the mailto form; the submit handler in `app.js`; the top bar's Contact link.
- [x] 4. Footer in `base.html` on every page; the drawer's Support link; the support card in `panel.html`.
- [x] 5. Tests for all of the above; suite green; ruff clean.
- [x] 6. Specs (`web.md`), scoping and roadmap rows; archive the change.
