# 009 — Tasks

Work in order. ∥ marks tasks that can run in parallel worktrees.

## 1. Metrics module ∥

- Shared aggregation used by `report` and the page: windows, per-stage
  percentiles, rates. Tests on seeded rows.

## 2. Dashboard page ∥

- `/admin` behind the secret; server-rendered tables and small charts;
  price-staleness line.

## 3. Weekly review

- Scheduled routine with the review prompt; writes `docs/reviews/`, opens
  a PR. Run once by hand to seed.

## 4. Specs, results, archive

- `docs/specs/monitoring.md`; scoping doc section 7 checklist updated.
